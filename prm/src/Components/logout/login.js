import React from 'react';
import Layout from "../../layout/Layout";

function login(props) {
    return (
        <Layout>
            This is login page
        </Layout>
    );
}

export default login;